pipeline {
    agent none
    options {
        timeout(time: 20, unit: 'MINUTES')
    }
    stages {
        stage('initialisation') {
            steps {
                script {
                    openshift.withCluster() {
                      openshift.withProject("akram-jenkins-sync-plugin-test") {
                         openshift.exec( "grafana-1-6dc89" , true, [ "ls" , "-R" , "/"])
                     }
                    }
                }
            }
        }
    }
}

